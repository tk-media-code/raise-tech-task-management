# PostgreSQL コンテナの初期化とバックエンドのDB接続に使う値。
# 使い方: このファイルをコピーして .env を作成する → cp .env.example .env
# .env は各自のローカル値（特に PASSWORD）を入れ、Git にはコミットしない。
POSTGRES_DB=taskmanagement
POSTGRES_USER=taskuser
POSTGRES_PASSWORD=change-me-locally
